syntax = "proto3";

package tonton;

option go_package = "github.com/ajayjogiya/tonton/proto";


message Chat{
    string message = 1;
    string from = 2;
    string to = 3;
}

service ChatService{
    rpc StreamChat(stream Chat) returns (stream Chat);
}
